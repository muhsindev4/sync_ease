name: sync_ease
description: "SyncEase is a lightweight Flutter State Management library designed to simplify state handling in Flutter applications. It offers intuitive APIs, error handling, loading state management, and batch operations, catering to various state management needs. Ideal for managing both simple value states and complex list states."
version: 0.0.2
homepage: "https://github.com/muhsindev4/sync_ease"

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
