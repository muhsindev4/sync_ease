name: sync_ease
description: "A lightweight Flutter State Management library for simplified state handling in Flutter applications."
version: 0.0.6
homepage: "https://github.com/muhsindev4/sync_ease"

environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1


flutter:
